---
name: n8n-api
description: Work with n8n API endpoints for workflows, executions, credentials, and automation control. Use when asked to query n8n resources, trigger workflows, inspect runs, or build API-based management scripts.
---

# n8n API

Use n8n HTTP API safely and consistently.

- Authenticate requests with API key/header.
- Read workflows/executions before mutating.
- Trigger runs and capture execution IDs.
- Return endpoint + payload + response summary.
