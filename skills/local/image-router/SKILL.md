---
name: image-router
description: Route and classify image assets by metadata, naming rules, and destination workflows. Use when asked to triage image batches, separate by type/source, prepare pipelines, or normalize image routing rules.
---

# Image Router

Classify image files into destination buckets.

- Route by extension/name pattern.
- Prepare folders for downstream processing.
- Generate a routing summary.
