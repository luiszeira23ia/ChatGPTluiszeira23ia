# Playbook: Scrape Lite

1. Open target page and snapshot.
2. Identify stable selectors for required data.
3. Extract minimal dataset only (avoid over-scraping).
4. Validate sample rows against visible UI.
5. Return structured output with source URL and timestamp.
