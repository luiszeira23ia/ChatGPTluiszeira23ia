# SQL Playbook: Incident
- Identify blast radius and affected queries.
- Stabilize (throttle/kill heavy query if approved).
- Collect evidence (locks, waits, plans, IO).
- Mitigate and define post-incident action.
